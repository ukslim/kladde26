source "https://rubygems.org"

# GitHub Pages builds this site natively; this gem pins local Jekyll and
# plugin versions to whatever GitHub Pages is currently running.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
